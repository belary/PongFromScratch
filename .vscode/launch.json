{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Windows Launch",
            "program": "${workspaceFolder}/main.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}